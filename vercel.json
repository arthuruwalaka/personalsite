{
  "buildCommand": "node build-prod.js && ng build --configuration production",
  "outputDirectory": "dist/my-personal-project/browser",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
